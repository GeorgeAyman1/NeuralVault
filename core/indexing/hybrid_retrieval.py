class HybridRetrieval:
    """
    Placeholder for Sprint 3 hybrid retrieval (semantic + keyword).

    Currently a passthrough — returns semantic results unchanged.
    """

    def rerank(self, query: str, semantic_results: list[dict]) -> list[dict]:
        return semantic_results
